# Deliberately no search engine indexing: the service is aimed at a
# small, known group of users, not at people searching for it. Without
# this rule, the Impressum with the operator's private address would,
# among other things, end up in search results. No sitemap.xml, because a
# sitemap would serve no purpose without wanting to be indexed.
#
# If the site should be findable later after all: remove "Disallow: /".

User-agent: *
Disallow: /
